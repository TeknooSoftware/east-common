language: php

sudo: false

php:
  - 8.0

matrix:
  include:
    - php: 8.0
    - php: 8.0
      env: dependencies=lowest

install:
  - composer self-update

before_script:
  - phpenv config-add mongodb.ini
  - make

script:
    - make qa
    - make test
